language: minimal

services:
  - docker

script:
  - docker build -t $TRAVIS_REPO_SLUG:travis-$TRAVIS_BUILD_NUMBER .

deploy:
  provider: script
  script: bash ci/docker-push.sh
  on:
    all_branches: true
